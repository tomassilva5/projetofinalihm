import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Etapa4Page } from './etapa4.page';

describe('Etapa4Page', () => {
  let component: Etapa4Page;
  let fixture: ComponentFixture<Etapa4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Etapa4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
