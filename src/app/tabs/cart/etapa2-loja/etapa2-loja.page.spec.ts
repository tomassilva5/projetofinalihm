import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Etapa2LojaPage } from './etapa2-loja.page';

describe('Etapa2LojaPage', () => {
  let component: Etapa2LojaPage;
  let fixture: ComponentFixture<Etapa2LojaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Etapa2LojaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
